import type { NextConfig } from "next";

/**
 * Next.js configuration — MVP application scaffold (issue #95, P0b).
 *
 * Deliberately minimal. This file selects no hosting provider, no runtime
 * target beyond the accepted long-running Node.js web service (ADR-005), and
 * no optional framework feature. Additions here are architecture decisions in
 * disguise; make them through the governed process, not by convenience.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
